@using CCS.Core
@using CCS.Common
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Index_Layout.cshtml";
}
@section styles{
<style>

</style>
 }
<div id="modalwindow" class="easyui-window" data-options="modal:true,closed:true,minimizable:false,shadow:false"></div>
<div class="mvctool">
    <input id="txtQuery" type="text" class="searchText">
    @Html.ToolButton("btnQuery", "icon-search", @Suggestion.Query, true)
    @Html.ToolButton("btnCreate", "icon-add", @Suggestion.Create, true)
    @Html.ToolButton("btnEdit", "icon-edit", @Suggestion.Edit, true)
    @Html.ToolButton("btnDetails", "icon-details", @Suggestion.Details, true)
    @Html.ToolButton("btnDelete", "icon-remove", @Suggestion.Delete, true)
    @Html.ToolButton("btnExport", "icon-export", @Suggestion.Export, true)
    @Html.ToolButton("btnReload", "icon-reload", @Suggestion.Reload, true)
    @Html.ToolButton("btnHyperlink", "icon-details", @Suggestion.Details, true)

</div>
<table id="List" style="width:100%;height:100%"></table>

@section scripts{

  <script type="text/javascript">
      $(function () {
          $(window).resize(function () {
              $('#List').datagrid('resize', {
                  width: $(window).width() - 10,
                  height: $(window).height() - 35
              }).datagrid('resize', {
                  width: $(window).width() - 10,
                  height: $(window).height() - 35
              });
          });

          

        $('#List').datagrid({
            url: '/Sales/SAL01/GetList',
            width: $(window).width() - 10,
            methord: 'post',
            height: $(window).height() - 35,
            fitColumns: true,
            sortName: 'VCH_NO',
            sortOrder: 'desc',
            idField: 'Id',
            pageSize: 20,
            pageList: [15, 20, 30, 40, 50],
            pagination: true,
            striped: true, // 奇偶行是否區分
            singleSelect: true,// 單選模式
            rownumbers: true,// 行號
            columns: [[
                { field: 'VCH_NO', title: '訂單號碼', width: 50 },
                { field: 'VCH_DT', title: '訂單日期', width: 50 },
                { field: 'FA_NO', title: '廠別', width: 30 },
                { field: 'CS_NO', title: '客戶', width: 30, align: 'right' },
                { field: 'CURRENCY', title: '幣別', width: 30, align: 'right' },
                { field: 'ITEM_NO', title: '品號', width: 60 },
                { field: 'PL', title: '鍍別', width: 60 },
                { field: 'UNIT', title: '單位', width: 30, align: 'center' },
                { field: 'QTY', title: '數量', width: 50, align: 'right' },
                { field: 'PRC', title: '單價', width: 50, align: 'right' },
                { field: 'AMT', title: '金額', width: 50 ,align:'right'},
                { field: 'RCV_QTY', title: '已交數量', width: 50, align: 'right' }
            ]]
        });

        var pager = $('#List').datagrid('getPager');
        $(pager).pagination({
            pageSize: 15,
            showPageList: true,
            pageList: [15, 20, 30, 40, 50],
            beforePageText: '第',
            afterPageText: '頁,共{pages}頁',
            displayMsg: '顯示 {from} 到 {to} 筆資料,共 {total} 筆資料'
        });

    });
</script>  

<script type="text/javascript">

    //ifram 返回
    function frameReturnByClose() {
        $("#modalwindow").window('close');
    }
    //iframe 返回並刷新
    function frameReturnByReload(flag) {
        if (flag)
            $("#List").datagrid('load');
        else
            $("#List").datagrid('reload');
    }
    // 輸出信息
    function frameReturnByMes(mes) {
        $.messageBox5s('提示', mes);
    }
    $(function () {
        $("#btnHyperlink").click(function () {
            var row = $('#List').datagrid('getSelected');
            if (row != null) {
                window.location="/Sales/SAL01/Details/"+row.VCH_NO;
            } else { $.messageBox5s('提示', '請選擇要操作的紀錄'); }
        });
           
     
        $("#btnReload").click(function () {
            frameReturnByReload();
        });
        $("#btnCreate").click(function () {
            $("#modalwindow").html("<iframe width='100%' height='98%' scrolling='no' frameborder='0'' src='/Sales/SAL01/Create'></iframe>");
            $("#modalwindow").window({ title: '新增', width: 850, height: 400, iconCls: 'icon-add' }).window('open');
        });
        $("#btnEdit").click(function () {
            var row = $('#List').datagrid('getSelected');
            if (row != null) {
                $("#modalwindow").html("<iframe width='100%' height='99%'  frameborder='0' src='/Sales/SAL01/Edit?id=" + row.VCH_NO + "&Ieguid=" + GetGuid() + "'></iframe>");
                $("#modalwindow").window({ title: '編輯' + row.VCH_NO, width: 700, height: 430, iconCls: 'icon-edit' }).window('open');
            } else { $.messageBox5s('提示', '請選擇要操作的紀錄'); }
        });
        $("#btnDetails").click(function () {
            var row = $('#List').datagrid('getSelected');
            if (row != null) {

                $("#modalwindow").html("<iframe width='100%' height='98%' scrolling='no' frameborder='0' src='/Sales/SAL01/Details?id=" + row.VCH_NO + "&Ieguid=" + GetGuid() + "'></iframe>");
                $("#modalwindow").window({ title: '詳細' + row.VCH_NO, width: 500, height: 300, iconCls: 'icon-details' }).window('open');
            } else { $.messageBox5s('提示', '請選擇要操作的紀錄'); }
            });
        $("#btnQuery").click(function () {
            var queryStr = $("#txtQuery").val();
            // 如果查詢條件為空,默認查詢全部
            if (queryStr == null) {
                queryStr = "%";
            }
            $('#List').datagrid({
                url: '/Sales/SAL01/GetList?queryStr=' + encodeURI(queryStr)
            });

        });
        $("#btnDelete").click(function () {
            var row = $('#List').datagrid('getSelected');
            if (row != null) {
                $.messager.confirm('提示', '@Suggestion.YouWantToDeleteTheSelectedRecords', function (r) {
                        if (r) {
                            $.post("/Sales/SAL01/Delete?id=" + row.VCH_NO, function (data) {
                                if (data == 1){
                                    $("#List").datagrid('reload');
                                    $.messageBox5s('提示', '刪除成功');
                                } else {
                                    $.messageBox5s('提示', '刪除失敗');
                                }
                            }, "json");

                        }
                    });
            } else { $.messageBox5s('提示', '請選擇要操作的記錄'); }
            });
    });

    //生成唯一的GUID
    function GetGuid() {
        var s4 = function () {
            return (((1 + Math.random()) * 0x10000) | 0).toString(16).substring(1);
        };
        return s4() + s4() + s4() + "-" + s4();
    }
</script>    
    }
